# Contributing

Open an issue before changing stable exports, compatibility rules, generated
inputs, or package metadata. Run
`pnpm --filter @torium-network/sdk verify` before requesting review.

Do not hand-edit `src/generated`, add product-backend dependencies, accept raw
wallet secret material, or add unapproved public RPC defaults.
